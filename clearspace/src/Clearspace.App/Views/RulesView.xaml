<UserControl x:Class="Clearspace.App.Views.RulesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Regeln" Style="{StaticResource PageTitle}" />
            <TextBlock Style="{StaticResource Caption}"
                       Text="Regeln lassen sich abschalten und in der Reihenfolge aendern. Deine manuellen Festlegungen und Schutzregeln stehen immer ueber automatischen Vorschlaegen." />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="320" />
            </Grid.ColumnDefinitions>

            <ListBox x:Name="RuleList" Grid.Column="0" ItemsSource="{Binding Rules}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,8" Margin="0,2" CornerRadius="8"
                                Background="{DynamicResource SurfaceElevatedBrush}"
                                BorderBrush="{DynamicResource BorderBrushSoft}" BorderThickness="1">
                            <StackPanel>
                                <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay}" Content="{Binding Name}" />
                                <TextBlock Style="{StaticResource Caption}" Text="{Binding EffectLabel}" />
                                <TextBlock Style="{StaticResource Caption}" Text="{Binding Description}" />
                                <TextBlock Style="{StaticResource Caption}"
                                           Text="{Binding AffectedCount, StringFormat=Betrifft aktuell {0} Eintraege}" />
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button Content="Vorschau" Click="Preview_Click" Tag="{Binding}" />
                                    <Button Content="Hoeher" Click="Up_Click" Tag="{Binding}" MinWidth="70" />
                                    <Button Content="Niedriger" Click="Down_Click" Tag="{Binding}" MinWidth="80" />
                                    <Button Content="Loeschen" Click="Delete_Click" Tag="{Binding}" MinWidth="80" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <ScrollViewer Grid.Column="1" Margin="14,0,0,0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Vorschau" Style="{StaticResource SectionTitle}" />
                    <ItemsControl ItemsSource="{Binding Preview}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}" Margin="0,2" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <TextBlock Grid.Row="2" Style="{StaticResource Caption}" Margin="0,8,0,0" Text="{Binding Status}" />
    </Grid>
</UserControl>
